#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main() {
    int n, cnt = 0;
    cin >> n;
    for(int i = 0; i<n; i++) {
        int x, y;
        cin >> x >> y;
        if(y-x>=2) {
            cnt++;
        }
    }
    cout << cnt << endl;



    return 0;
}
