#include <iostream>
#include <vector>
#include <set>
#include <algorithm>
#include <utility>
#include <iterator>
using namespace std;
int main() {
    int k, l, m, n, d;
    cin >> k >> l >> m >> n >> d;
    set<int> arr;
    set<int>::iterator it;
    for(int i = k; i<=d; i+=k) {
        arr.insert(i);
    }
    for(int i = l; i<=d; i+=l) {
        arr.insert(i);
    }
    for(int i = m; i<=d; i+=m) {
        arr.insert(i);
    }
    for(int i = n; i<=d; i+=n) {
        arr.insert(i);
    }
    cout << arr.size() << endl;


    return 0;
}

